"use client";

/**
 * Command Palette — JAG-XXX (to be implemented)
 *
 * Triggered by Cmd+K / Ctrl+K globally.
 * Will support:
 * - Navigation (go to /movies, /under-the-hood, etc.)
 * - Theme toggle (light / dark / system)
 * - CV download
 *
 * Uses useCommandPalette() hook for keyboard binding and state management.
 * UI will be built with HeroUI's Modal + a searchable list.
 *
 * 🥚 EASTER EGG: Drop a hidden console.log in the Providers so devs who
 *    inspect the console see a friendly message. See app/layout.tsx.
 */

export function CommandPalette() {
  // TODO (JAG-XXX): Implement command palette UI
  return null;
}
